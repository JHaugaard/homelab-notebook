# PocketBase URLs
# Development: local Docker container
# Production: your hosted PocketBase instance
# Internal URL (for server-side requests within Fly.io network)
PUBLIC_POCKETBASE_URL=http://localhost:8090
# Public URL (for browser requests)
PUBLIC_POCKETBASE_PUBLIC_URL=http://localhost:8090

# Tigris (S3-compatible) Storage
# These are set automatically by Fly.io when you run `fly storage create`
# For local development, leave blank or use a local MinIO instance
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_ENDPOINT_URL_S3=https://fly.storage.tigris.dev
AWS_REGION=auto
BUCKET_NAME=homelab-notebook-files
